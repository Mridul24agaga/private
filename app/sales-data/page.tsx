// Import the SalesData component from its file
import SalesData from '@/components/sales-data'; // Adjust the path as needed

const Page = () => {
  return (
    <div>
      <SalesData />
    </div>
  );
};

export default Page;
